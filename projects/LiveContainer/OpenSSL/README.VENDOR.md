# OpenSSL (not vendored)

Upstream submodule: https://github.com/krzyzanowskim/OpenSSL

Prebuilt frameworks are ~360MB and are omitted from this study snapshot.
To build LiveContainer yourself, init the submodule from a full upstream checkout:

```bash
git submodule update --init OpenSSL
```
