# getpid 系统调用路径（笔记模板）

> 状态：待填写。按 [学习计划 · 阶段 1](../docs/LEARNING_PLAN.md) 边读源码边补全。

## 1. 用户意图

用户态调用 `getpid()`，期望得到当前进程 PID。

## 2. 用户态

- 头文件 / 声明：
- 实际落入的 stub / 指令（arm64 `svc` 或 x86_64 `syscall`）：
- 寄存器约定（编号、参数、返回值）：
- syscall 编号：

## 3. 内核入口

- 陷阱 / 异常入口符号：
- 如何识别为 BSD syscall（而非 Mach trap）：
- 分发函数：

## 4. 查表

- 表结构（如 `sysent[]`）：
- `syscalls.master` 中对应行：
- handler 函数名：

## 5. handler 实现

- 源文件路径：
- 如何取得当前 `proc`：
- 返回值如何写回用户态：
- 是否需要 `copyin`/`copyout`：（通常否）

## 6. 返回路径

- 成功返回：
- 失败 / `errno`：（若适用）

## 7. 观测验证

- `dtruss` / DTrace 输出摘要：
- 与源码编号是否一致：

## 8. 对照

| 项目 | BSD `getpid` | 某个 Mach trap（自选） |
|------|--------------|------------------------|
| 用户态入口 | | |
| 内核表 | | |
| 返回内容 | PID | |

## 9. 未决问题

-
