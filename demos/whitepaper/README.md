# Syscall 白皮书配套 Demo

对应文档：[docs/SYSCALL_SECURITY_WHITEPAPER.md](../../docs/SYSCALL_SECURITY_WHITEPAPER.md)

```bash
make check           # 可移植（Linux/macOS）
make check-darwin    # 仅 macOS（Mach / csops）
```

编号 `01–14` 可移植；`20–23` 需 Darwin。
