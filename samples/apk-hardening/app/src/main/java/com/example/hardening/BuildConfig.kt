package com.example.hardening

/**
 * 占位说明：真实工程由 Android Gradle Plugin 生成 BuildConfig。
 * 接入时删除本文件，改用 AGP 生成的字段（见 app/build.gradle.kts）。
 */
object BuildConfig {
    const val EXPECTED_CERT_SHA256: String = ""
    const val HARDENING_STRICT: Boolean = false
}
